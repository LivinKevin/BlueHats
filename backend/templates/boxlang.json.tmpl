{
	// Generated by Agent Factory. Run this project via the repo-root wrapper:
	//   ./bx.ps1 generated/@@SLUG@@/run.bxs
	// which loads .env and passes --bx-config <repo>/boxlang.json, so this file
	// is mostly a record of intent — the wrapper's config wins at runtime.
	"modules": {
		"bxai": {
			"settings": {
				"provider": "@@PROVIDER@@",
				"defaultParams": { "model": "@@MODEL@@", "max_tokens": 2000, "temperature": 0.2 },
				"returnFormat": "single"
			}
		}
	}
}
