@@NAME@@ = aiTool(
	"@@NAME@@",
	@@DESCRIPTION@@,
	( @@PARAMS@@ ) => {
		// TODO(agent-factory): no real implementation wired in yet - said honestly below
		// instead of returning fabricated-looking data.
		return "[SIMULATED - no live data source connected yet] " & @@BODY@@;
	}
)@@DESCRIBE_CALLS@@;
