# @@AGENT_NAME@@

Generated by **Agent Factory** from this problem statement:

> @@PROBLEM@@

## Role

@@ROLE_PLAIN@@

## Tools

@@TOOL_TABLE@@

## Run

This is a [BxAgents](https://bxagents.ai) project (`Agent.bx` + `tools/`, discovered
by convention). From this folder:

```bash
bxAgents build
bxAgents chat
```

Or a single non-interactive turn (needs a real provider key in your environment):

```bash
bxAgents invoke --message="..."
```

## Status

Tool bodies that could be implemented for real (pure computation, or reading this
machine's own local state - no third-party API involved) already contain working
code. Anything left as `[SIMULATED - no live data source connected yet]` or
`[NEEDS REAL API]` genuinely needs a real external API key wired in before it
returns real data - see the `TODO(agent-factory)` comment in that tool's file under
`tools/`.
