# @@AGENT_NAME@@

Generated by **Agent Factory** from this problem statement:

> @@PROBLEM@@

## Role

@@ROLE_PLAIN@@

## Tools

@@TOOL_TABLE@@

## Run

```bash
./bx.ps1 generated/@@SLUG@@/run.bxs
```

```bash
./bx.ps1 generated/@@SLUG@@/run.bxs --mock
```

`--mock` uses bx-ai's mock provider: no API key, deterministic output — used by the
Factory's validation step to prove the project runs.

## Status

Tool bodies are **stubs** (`TODO(agent-factory)`). The wiring — agent, tools,
parameters, instructions, memory — is complete and runnable. Fill in the stubs to
make it do real work.
